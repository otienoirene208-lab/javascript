const simpleInterest =(p,r,t) => {
    ans = (p*r*t) /100 
    console.log(`the simple interest of  USD ${p} after ${t} at the rate of ${r}%  will  be ${ans}`)
}
simpleInterest(40,5,2);


// workout
const subtraction =(num1,num2) =>{
    ans(num1-num2)
    console.log('')
}